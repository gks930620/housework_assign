name: untitled
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ^3.5.3
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  kakao_flutter_sdk_user: ^1.5.0  # kakao
  http: ^0.13.6  # 또는 최신 버전 사용
  flutter_secure_storage: ^8.0.0
  flutter_riverpod: ^2.4.0   # ✅ 이 라인을 추가
  google_sign_in: ^6.1.5  # ✅ 최신 버전 명시

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/kakao_logo.png
    - assets/google_logo.png
